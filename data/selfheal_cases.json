{
  "orders": [
    {
      "case_id": "orders_country_null",
      "target_column": "country",
      "failing_rule_type": "not_null",
      "statement": "country must not be null",
      "total_rows": 482301,
      "failing_row_count": 13987,
      "sample_failing_values": [null, null, null],
      "hint": "customer_id repeats across orders, so country is recoverable from the same customer's other rows — a reference_lookup beats a mode_fill here."
    },
    {
      "case_id": "orders_total_amount_negative",
      "target_column": "total_amount",
      "failing_rule_type": "non_negative",
      "statement": "total_amount must be >= 0",
      "total_rows": 482301,
      "failing_row_count": 412,
      "sample_failing_values": [-120.0, -35.5, -9.99],
      "hint": "negatives are refund rows where discount exceeded quantity*unit_price; recomputing from the formula is safer than nulling."
    }
  ],
  "sales_transactions": [
    {
      "case_id": "txn_state_null",
      "target_column": "state",
      "failing_rule_type": "not_null",
      "statement": "state must not be null",
      "total_rows": 219884,
      "failing_row_count": 7476,
      "sample_failing_values": [null, null],
      "hint": "cross-table transfer target: buyer_id plays the same role customer_id does in orders."
    }
  ]
}
